# Ajustar context TIC a Jobs

## Goal
Fer que `TIC/ATIC` es mostri com a `Àrea TIC` i que `Lots amb ruta` ensenyi també els correus.

## Tasks
- [ ] Reclassificar la ruta `TIC` perquè no aparegui a `Contextos especials` → Verify: `ATIC` surt només a `Àrea TIC`.
- [ ] Mostrar els correus sota cada lot a `Lots amb ruta` → Verify: cada lot mostra la seva llista de correus.
- [ ] Actualitzar el test del panell de Jobs → Verify: `vitest` passa.
- [ ] Reconstruir frontend → Verify: `npm run build` passa.

## Done When
- [ ] `Àrea TIC` agrupa el correu resum i la ruta `ATIC`.
- [ ] `Contextos especials` deixa `PROVES`.
- [ ] `Lots amb ruta` mostra correus.
