# Arrencada del Dashboard d'Auditoria E13BD

## Goal
Inicialitzar i posar en marxa el sistema d'auditoria (Backend FastAPI + Frontend React) per permetre l'anàlisi de BBDD Oracle.

## Tasks
- [x] Task 1: Verificar entorn virtual de Python → Verify: `& .venv\Scripts\Activate.ps1; pip list`
- [x] Task 2: Verificar o generar el build del frontend a `src/web-app/dist` → Verify: Existència de `index.html` a `dist/`
- [x] Task 3: Llançar el backend FastAPI → Verify: Execució de `uvicorn src.api.main:app` al port 8000
- [x] Task 4: Verificar l'accés al Dashboard → Verify: Navegar a `http://127.0.0.1:8000` i veure la UI

## Done When
- [x] El backend està corrent sense errors.
- [x] La interfície web és accessible i funcional des del navegador.
